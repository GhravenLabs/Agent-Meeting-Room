{
  "name": "agent-meeting-room-tauri-shell",
  "version": "1.9.0",
  "private": true,
  "description": "Tauri desktop shell that owns the Agent Meeting Room Flask backend.",
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build --target x86_64-pc-windows-msvc"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
